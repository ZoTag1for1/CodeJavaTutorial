class StringLength {
    public static void main(String[] args) {
        String name = "Computer";
        int stringSize;
        stringSize = name.length();
        System.err.println(name+" has " +stringSize+ " characters");
    }
}
